import { FormatLastnamePipe } from './format-lastname.pipe';

describe('FormatLastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
